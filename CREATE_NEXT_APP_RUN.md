# create-next-app run log

Attempted to run the requested command in this repository:

```bash
npx create-next-app@latest . --yes
```

Result:

- Command failed with `npm ERR! code E403` due registry access being forbidden in this environment.
- No project files were regenerated by `create-next-app`.

Raw output excerpt:

```text
npm error code E403
npm error 403 403 Forbidden - GET https://registry.npmjs.org/create-next-app
```
